# 🛫 Сравнение аэропортов вылета

Эта функциональность позволяет сравнивать цены на путешествия из разных аэропортов вылета.

## 🎯 Что делает

1. **Запускает два поиска для каждой страны:**
   - Из Варшавы (основной фильтр)
   - Из всех аэропортов (без привязки к конкретному аэропорту)

2. **Сравнивает результаты и находит:**
   - Отели до 8000 PLN, которые есть в любых аэропортах, но отсутствуют в Варшаве
   - Отели, которые дешевле из других аэропортов, чем из Варшавы

3. **Создает отчеты:**
   - JSON файл с детальными результатами сравнения
   - Текстовый отчет для чтения
   - Обновляет дашборд с новой информацией

## 📁 Файлы

### Конфигурационные файлы
- `config_any_airports.json` - Греция, все аэропорты
- `config_egypt_any_airports.json` - Египет, все аэропорты  
- `config_turkey_any_airports.json` - Турция, все аэропорты

### Основные скрипты
- `airport_comparison.py` - Модуль сравнения аэропортов
- `travel_monitor_with_airport_comparison.py` - Мониторинг одной страны с сравнением
- `run_all_countries_with_airport_comparison.py` - Мониторинг всех стран с сравнением

### Тестовые файлы
- `test_airport_comparison.py` - Тестирование функциональности

## 🚀 Использование

### Запуск для одной страны
```bash
python3 travel_monitor_with_airport_comparison.py --base-config config.json --any-airports-config config_any_airports.json
```

### Запуск для всех стран
```bash
python3 run_all_countries_with_airport_comparison.py
```

### Тестирование
```bash
python3 test_airport_comparison.py
```

## 📊 Результаты

### JSON файлы с результатами
- `{country}_airport_comparison.json` - Детальные результаты сравнения
- `all_countries_monitoring_summary.json` - Сводка по всем странам

### Текстовые отчеты
- `{country}_airport_comparison_report.txt` - Отчет по стране
- `all_countries_monitoring_summary.json` - Сводный отчет

### Новые поля в данных
- `departure_airport` - Аэропорт вылета (Warszawa, Kraków, Gdańsk, и т.д.)

## 🔧 Настройка

### URL для поиска из всех аэропортов
В конфигурационных файлах URL должен содержать пустой параметр `filter[from]=`:

```
https://fly.pl/kierunek/grecja/?filter[dest]=14:&filter[from]=&filter[person]=2...
```

### Лимит цены
Для поиска из всех аэропортов установлен лимит 30000 PLN (вместо 8100 для Варшавы), чтобы найти больше вариантов.

## 📈 Дашборд

Дашборд будет обновлен для отображения:
1. **Таблицы с отелями до 8000 PLN**, которые есть в любых аэропортах, но отсутствуют в Варшаве
2. **Новые колонки в основной таблице:**
   - Аэропорт вылета
   - Лучшее предложение (если есть дешевле из других аэропортов)

## 🧪 Тестирование

Тестовый скрипт создает примеры данных и демонстрирует работу функциональности:

```bash
python3 test_airport_comparison.py
```

Результат:
- 2 отеля отсутствуют в Варшаве (Hotel D, Hotel E)
- 1 отель дешевле из других аэропортов (Hotel A: экономия 500 PLN)

## 📝 Логи

Все операции логируются в файлы:
- `monitor_with_airport_comparison.log` - Логи мониторинга с сравнением
- `all_countries_monitor.log` - Логи мониторинга всех стран
